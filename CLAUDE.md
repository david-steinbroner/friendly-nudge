# Friendly Nudge

Standards: inherits ../engineering-standards.md. Overrides & project-specifics below.

**Stack:** see `README.md` (app sources in `FriendlyNudge/`, helper scripts in `scripts/`).

## Commands
- dev / build / test: see `README.md` and `CONTRIBUTING.md` for the current run/build/test commands

## Commit protocol
Ask before committing (see ../CLAUDE.md). Licensed (`LICENSE`) — keep headers intact.

## Version tag
Surface version in the UI per ../CLAUDE.md; bump on each meaningful change.

## Project-specific rules & overrides
- Resume context from `README.md` + `docs/`; follow `CONTRIBUTING.md` for contribution conventions.
